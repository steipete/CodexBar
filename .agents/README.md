# CodexBar Agent Notes

这个目录存放仓库内给代理/自动化使用的补充说明，目标是让操作约定比 `AGENTS.md` 更聚焦、更容易同步维护。

当前建议读取顺序：
- `./release-update.md`：Sparkle、发布、upstream 同步、l10n 同步相关约定。
- `../AGENTS.md`：通用仓库规则、验证要求、本机签名与打包约定。

维护原则：
- 当发布链路、工作流、路径、签名方式、集中配置位置发生变化时，要同步更新这里。
- 这里写“当前事实”和“最小操作步骤”，不要写过期方案或历史背景长文。
